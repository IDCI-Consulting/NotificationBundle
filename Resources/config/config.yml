da_api_client:
    log_enabled: true
    api:
        notification:
            endpoint_root: %api.notification.endpoint_root%
            security_token: %api.security_token%
            cache_enabled: true